<?php

//var_dump($args_array);


$level = 0;
$template = 'simple';

foreach($args_array as $arg) {
	if (is_numeric($arg)) {
		$level = $arg;
	} else {
		$template = $arg;
	}
}

$nav = CMS::navigation()->level($level);
if ($nav) {
	print $nav->draw($template);
}