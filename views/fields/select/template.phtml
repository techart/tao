<?php
	//$tagparms['name'] = $form->name."[$name]";
	print $this->forms->select($_name, $tagparms);
	/*foreach($items as $value => $title) {
		$opt = array('value' => $value);
		if ($value==$form[$name]) $opt['selected'] = true;
		print $this->tags->tag('option',$opt,false);
		print $title;
		print '</option>';
	}
	print '</select>';*/
