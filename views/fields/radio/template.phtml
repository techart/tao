<?php
	$fname = $form->name."[$name]";
	$tagparms['type'] = 'radio';
	$tagparms['name'] = $fname;
	foreach($items as $key => $title) {
		$tagparms['value'] = $key;
		unset($tagparms['checked']);
		if ($form[$name]==$key) $tagparms['checked'] = true; ?>
		<label><?= $this->tags->tag('input',$tagparms); ?>
		<span><?=$title?>&nbsp;</span></label>
	<?php }