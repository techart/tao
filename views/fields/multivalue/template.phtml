<div class="field field-multivalue">
<?php if(!$item->is_phantom()) {
	print $this->partial($type_object->template($data, 'items'));
	print $this->partial($type_object->template($data, 'add'), array('item' => $item));
} else {
	$lang = CMS::lang()->_common;
	print $lang->unable_in_add;
} ?>
</div>
